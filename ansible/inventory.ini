[local]
localhost ansible_connection=local

[local:vars]
ansible_python_interpreter=/usr/bin/python3

# --- For real EC2 use (Phase 5 reference) ---
# [webservers]
# <EC2_PUBLIC_IP> ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/devops-app-key.pem
