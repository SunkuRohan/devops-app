server.port=8080

# App custom properties — override these via env vars in Docker/K8s
app.version=1.0.0
app.environment=local

# Spring Actuator — exposes /actuator/health used by K8s probes
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always
